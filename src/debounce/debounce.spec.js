'use strict';

describe('truelab.debounce', function () {

    beforeEach(module('truelab.debounce'));

    beforeEach(inject(function () {

    }));

    it('should load the module', function () {
        expect(true).toBe(true);
    });
});